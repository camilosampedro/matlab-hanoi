function move_disk( hanoi_pins, source_pin, destination_pin )
% Moves a pin from a pin to another pin
%   Detailed explanation goes here
disk_index = get_higher_disk(hanoi_pins(:, source_pin));

end

function [ disk_index ] = get_higher_disk_position(pin)
end

function put_disk(pin, disk)
end

